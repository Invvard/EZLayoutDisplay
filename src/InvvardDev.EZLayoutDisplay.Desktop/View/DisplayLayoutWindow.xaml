<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:command="http://www.galasoft.ch/mvvmlight"
    x:Class="InvvardDev.EZLayoutDisplay.Desktop.View.DisplayLayoutWindow"
    mc:Ignorable="d" Height="450" Width="800"
    WindowStartupLocation="CenterScreen" Topmost="True"
    ResizeMode="NoResize"
    Visibility="Visible" ShowInTaskbar="False" WindowStyle="None"
    Title="{Binding WindowTitle}"
    DataContext="{Binding DisplayLayout, Source={StaticResource Locator}}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Deactivated">
            <command:EventToCommand Command="{Binding LostFocusCommand, Mode=OneWay}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding LostFocusCommand, Mode=OneWay}" />
    </Window.InputBindings>

    <ItemsControl ItemsSource="{Binding RectItems}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding X}" />
                <Setter Property="Canvas.Top" Value="{Binding Y}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Canvas>
                    <Rectangle x:Name="KeyBase" Height="81" Width="54" HorizontalAlignment="Left" Canvas.Left="20"
                               Canvas.Top="20"
                               RenderTransformOrigin="0,0" StrokeThickness="0" Style="{DynamicResource KeyBase}" />
                    <Rectangle x:Name="KeyTop" StrokeThickness="0" Canvas.Left="23" Canvas.Top="23" Width="48"
                               Height="70"
                               Style="{DynamicResource KeyTop}" />
                </Canvas>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</Window>